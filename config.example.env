# Generated by solo-dev-ai-kit/bootstrap.sh — safe to commit (metadata only, no tokens).
GH_REPO=owner/repo
GH_PROJECT_NUM=                    # Set by gh-setup-project.sh
GH_PROJECT_OWNER=@me
GH_PROJECT_TITLE=My Project delivery
INTEGRATION_BRANCH=dev
PRODUCTION_BRANCH=main
SINGLE_BRANCH=false                 # true = single branch (no separate dev)
CI_TEST_COMMAND=run tests
WORKFLOW_TOOLS=cursor,antigravity,codex,claude,gemini
HAS_CLIENT_REPORTS=false
APP_STACK=web                       # web | mobile | both
VERIFY_MAX_ROUNDS=3                 # Override per issue in chat: "max 2 rounds"
