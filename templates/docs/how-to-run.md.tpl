# How to run this app (fill in once)

AI agents read this file before UI verification. **Update the placeholders below.**

## Dev server

| Item | Value |
|------|--------|
| **Start command** | `<!-- e.g. npm run dev -->` |
| **Local URL** | `<!-- e.g. http://localhost:3000 -->` |
| **Health check** | `<!-- e.g. curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 -->` |

## Tests

| Item | Value |
|------|--------|
| **Test command** | `{{CI_TEST_COMMAND}}` |
| **Focused test example** | `<!-- e.g. npm test -- path/to/test -->` |

## Test accounts (if login required)

| Role | Email / user | Password / notes |
|------|----------------|------------------|
| Demo user | `<!-- fill in -->` | `<!-- fill in -->` |

## Notes

- Staging URL (optional): `<!-- e.g. https://preview.example.com -->`
- Env file: copy `.env.example` → `.env` (never commit secrets)
- Known quirks: `<!-- e.g. must run migrations first -->`
